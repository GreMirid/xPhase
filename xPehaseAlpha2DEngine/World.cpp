#include "world.h"

namespace xphase
{
	int World::create()
	{
		//TASK:
		/// Get gata from conteiner and write to RAM
		/// Data is info about scenes and this objects

		return EXIT_OK;
	}

	void World::update(double delta, int playerLocation)
	{
		//scenes[playerLocation].update(delta);
	}
}