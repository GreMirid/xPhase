#include "world.h"

namespace xphase
{
	int World::create()
	{
		//TASK:
		/// Get data from conteiner and write to RAM
		/// Is data info about scenes and this objects

		return EXIT_OK;
	}

	void World::update(double delta, int playerLocation)
	{
		//scenes[playerLocation].update(delta);
	}
}